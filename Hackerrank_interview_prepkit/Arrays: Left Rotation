vector<int> rotLeft(vector<int> a, int d) {
    
    vector<int> ans;
    int n=a.size();
    for(int i=d;i<n;i++){
        ans.push_back(a[i]);
    }
    for(int i=0;i<d;i++){
        ans.push_back(a[i]);
    }
    return ans;
}
